{
  "name": "inventario-oficina",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "setup": "uv sync --project backend --link-mode=copy && npm --prefix frontend install",
    "build:frontend": "npm --prefix frontend run build",
    "build:exe": "powershell -ExecutionPolicy Bypass -File .\\build_exe.ps1",
    "dev": "concurrently -n backend,frontend -c green,cyan \"backend\\\\.venv\\\\Scripts\\\\python.exe -m uvicorn app.main:app --app-dir backend --reload --port 8000\" \"npm --prefix frontend run dev\"",
    "dev:backend": "backend\\\\.venv\\\\Scripts\\\\python.exe -m uvicorn app.main:app --app-dir backend --reload --port 8000",
    "dev:frontend": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
